<?xml version="1.0" encoding="UTF-8"?>
<Configure class="org.mortbay.jetty.webapp.WebAppContext">

  <!-- For some reason, it do not use system classpath, meaning I have to put this manually. -->
  <Set name="extraClasspath">/usr/share/java/postgresql-jdbc.jar</Set>

  <!-- Configure data source mapping -->
  <New id="datasource" class="org.mortbay.jetty.plus.naming.Resource">
    <Arg>jdbc/default</Arg>
    <Arg>
      <New class="org.apache.commons.dbcp.BasicDataSource">
        <Set name="driverClassName">org.postgresql.Driver</Set>
        <Set name="url">jdbc:postgresql:katmana</Set>
        <Set name="username">katmana</Set>
        <Set name="password">katmana</Set>
        <Set name="validationQuery">SELECT 1</Set>
      </New>
    </Arg>
  </New>

</Configure>